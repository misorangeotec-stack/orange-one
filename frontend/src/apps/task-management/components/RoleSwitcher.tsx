/**
 * RoleSwitcher moved to the portal core (core/platform/RoleSwitcher) now that the
 * session is portal-wide. Re-exported here so existing imports keep resolving.
 */
export { default } from "@/core/platform/RoleSwitcher";
